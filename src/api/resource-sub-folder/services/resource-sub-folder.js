'use strict';

/**
 * resource-sub-folder service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::resource-sub-folder.resource-sub-folder');
